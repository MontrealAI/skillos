name: Autonomous RSI Revenue Experiment Factory Market Proof

on:
  workflow_dispatch:
  push:
    branches: [main]
    paths:
      - "scripts/run_rsi_revenue_experiment_factory_market_proof.py"
      - "scripts/make_rsi_revenue_experiment_factory_proof_visible.py"
      - "scripts/verify_rsi_revenue_experiment_factory_proof.py"
      - ".github/workflows/rsi-revenue-experiment-factory-proof.yml"
  schedule:
    - cron: "43 13 * * *"

permissions:
  contents: write
  pages: write
  id-token: write
  actions: read

concurrency:
  group: "pages"
  cancel-in-progress: false

jobs:
  proof-and-deploy:
    name: Run RSI revenue experiment factory proof and deploy visual dashboard
    runs-on: ubuntu-latest
    environment:
      name: github-pages
      url: https://montrealai.github.io/skillos/
    steps:
      - name: Check out repository
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.x"

      - name: Run autonomous RSI revenue experiment factory market proof
        run: python scripts/run_rsi_revenue_experiment_factory_market_proof.py

      - name: Make proof visible from homepage
        run: python scripts/make_rsi_revenue_experiment_factory_proof_visible.py

      - name: Verify proof visibility
        run: python scripts/verify_rsi_revenue_experiment_factory_proof.py

      - name: Apply safe public copy if available
        run: python scripts/apply_safe_public_copy.py || true

      - name: Build GitHub Pages site
        run: |
          if [ -f scripts/build_pages.py ]; then
            python scripts/build_pages.py
          else
            mkdir -p dist
            cp -R site/* dist/
          fi

      - name: Upload proof artifacts
        uses: actions/upload-artifact@v4
        with:
          name: skillos-rsi-revenue-experiment-factory-proof
          path: |
            site/rsi-revenue-experiment-factory-proof.html
            docs/rsi_revenue_experiment_factory_market_proof.md
            data/rsi_revenue_experiment_factory_market_proof.json
            data/rsi_revenue_experiment_factory_benchmark.json
            badges/rsi_revenue_experiment_factory_market_proof.svg

      - name: Commit generated proof outputs
        continue-on-error: true
        run: |
          if [ -n "$(git status --porcelain)" ]; then
            git config user.name "github-actions[bot]"
            git config user.email "github-actions[bot]@users.noreply.github.com"
            git add site/rsi-revenue-experiment-factory-proof.html site/index.html docs/rsi_revenue_experiment_factory_market_proof.md data/rsi_revenue_experiment_factory_market_proof.json data/rsi_revenue_experiment_factory_benchmark.json badges/rsi_revenue_experiment_factory_market_proof.svg
            git commit -m "Update autonomous RSI revenue experiment factory proof"
            git push
          else
            echo "No RSI revenue experiment factory proof changes to commit."
          fi

      - name: Configure GitHub Pages
        uses: actions/configure-pages@v5

      - name: Upload GitHub Pages artifact
        uses: actions/upload-pages-artifact@v3
        with:
          path: dist

      - name: Deploy to GitHub Pages
        uses: actions/deploy-pages@v4
