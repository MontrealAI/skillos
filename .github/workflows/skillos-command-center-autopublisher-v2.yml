name: Retired legacy SkillOS site refresh

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  retired:
    name: Retired; v5 canonical deploy is the single publisher
    runs-on: ubuntu-latest
    steps:
      - name: Explain retirement
        run: |
          echo "This legacy workflow is intentionally retired."
          echo "Use: SkillOS Sovereign Command Center v5 Canonical Deploy."
