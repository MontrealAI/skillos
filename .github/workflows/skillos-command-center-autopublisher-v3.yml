name: Retired legacy SkillOS site workflow

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  retired:
    runs-on: ubuntu-latest
    steps:
      - run: |
          echo "This legacy workflow is retired."
          echo "Use: Public SkillOS Command Center Root Authority v7.1."
