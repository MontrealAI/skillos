# SkillOS Wealth-Accumulation Proof

This repository now proves the core claim:

**One workflow gets cheaper, faster, and better as agents do the work.**

The workflow is sales follow-up emails from call notes.

## Fastest proof

Run:

```bash
python -m skillos.cli wealth-proof
```

Expected result:

```text
quality rises
time per job falls
cost per job falls
proof gates pass
```

## What makes it convincing

The proof is generated from code, not hard-coded website copy.

GitHub Actions runs:

```text
repository verification
unit tests
SkillOS verification loop
wealth proof
GitHub Pages build
GitHub Pages verification
```

The website publishes the generated proof as:

```text
data/wealth_proof.json
```

## The wealth loop

```text
Work → Trace → Lesson → Skill → Test → Release → Better Work → Lower Cost → Higher Margin
```

That is the wealth-accumulation layer.
